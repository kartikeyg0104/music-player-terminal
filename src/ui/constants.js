/** Values shared between the layout, the keymap and the action layer. */
export const VERSION = '1.0.0';

/** Offered by the sleep-timer dialog. */
export const SLEEP_TIMER_OPTIONS = [5, 10, 15, 30, 45, 60, 90];

/** Minimum usable terminal geometry. Below this the UI shows a resize prompt. */
export const MIN_COLUMNS = 54;
export const MIN_ROWS = 16;

/** Below this width the sidebar collapses into a tab strip. */
export const COMPACT_BREAKPOINT = 92;
